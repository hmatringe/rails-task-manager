 <%= form_for(@task) do |f| %>
 <%= f.label :name %>
 <%= f.text_field :name %>
 <br>

 <%= f.label :description %>
 <%= f.text_field :description %>
 <br>

 <%= f.submit 'Submit', class: "btn btn-success" %>
 <% end %>
 <br>
 <%= link_to "Back to home", tasks_path %>
